# grooveback

Auto-loaded every session. Its only job is to pull the project's operating knowledge into context eagerly — the file
it imports is skill-formatted, so it is also invocable by name from anywhere.

@.claude/skills/operate-grooveback/SKILL.md

Cross-project working agreements are **not** here. They live in `~/.claude/CLAUDE.md`, which applies to every
project; `docs/CLAUDE.md` is the versioned copy of that file, parked until this becomes a monorepo and it moves to
the root.

Findings live in `docs/decisions/`. Do not restate their conclusions elsewhere.
