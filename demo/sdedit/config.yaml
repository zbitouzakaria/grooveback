# The instrument: every sdedit render of every pack — the noise sweep, the
# theta sweep, and the prompted variant — behind the four anchors, one player
# per sweep, created only when its section is opened.
#   uv run --group notebooks python scripts/build_demo.py demo/sdedit/config.yaml

aerofunk_32k:
  dir: artifacts/sdedit/listen/aerofunk_32k
  tracks: all

aerofunk_64k:
  dir: artifacts/sdedit/listen/aerofunk_64k
  tracks: all

codec_32k:
  dir: artifacts/sdedit/listen/codec_32k
  tracks: all

codec_64k:
  dir: artifacts/sdedit/listen/codec_64k
  tracks: all
