# The curated comparison: input / master / apollo / picked sdedits, per
# (source x bitrate). Edit the picks as the listening verdict forms; new
# methods get added here. Rebuild with
#   uv run --group notebooks python scripts/build_demo.py demo/base/config.yaml

aerofunk_64k:
  dir: artifacts/sdedit/listen/aerofunk_64k
  window_s: [0.0, 90.0]
  tracks:
    input: input.flac
    master: original.flac
    apollo: apollo.flac
    sdedit small s8 0.16: small-music_s8_0.16.flac
    sdedit medium s8 0.16: medium_s8_0.16.flac

aerofunk_128k:
  dir: artifacts/sdedit/listen/aerofunk_128k
  window_s: [0.0, 90.0]
  tracks:
    input: input.flac
    master: original.flac
    apollo: apollo.flac
    sdedit small s8 0.16: small-music_s8_0.16.flac
    sdedit medium s8 0.16: medium_s8_0.16.flac

aerofunk_192k:
  dir: artifacts/sdedit/listen/aerofunk_192k
  window_s: [0.0, 90.0]
  tracks:
    input: input.flac
    master: original.flac
    apollo: apollo.flac
    sdedit small s8 0.16: small-music_s8_0.16.flac
    sdedit medium s8 0.16: medium_s8_0.16.flac

codec_64k:
  dir: artifacts/sdedit/listen/codec_64k
  tracks:
    input: input.flac
    master: original.flac
    apollo: apollo.flac
    sdedit small s8 0.16: small-music_s8_0.16.flac
    sdedit medium s8 0.16: medium_s8_0.16.flac

codec_128k:
  dir: artifacts/sdedit/listen/codec_128k
  tracks:
    input: input.flac
    master: original.flac
    apollo: apollo.flac
    sdedit small s8 0.16: small-music_s8_0.16.flac
    sdedit medium s8 0.16: medium_s8_0.16.flac

codec_192k:
  dir: artifacts/sdedit/listen/codec_192k
  tracks:
    input: input.flac
    master: original.flac
    apollo: apollo.flac
    sdedit small s8 0.16: small-music_s8_0.16.flac
    sdedit medium s8 0.16: medium_s8_0.16.flac
