# The curated comparison: input / master / apollo / a2sb / picked sdedits, per
# (source x bitrate) — the full tracks, straight from the listen packs.
# Edit the picks as the listening verdict forms; new methods get added here.
#   uv run --group notebooks python scripts/build_demo.py demo/base/config.yaml

aerofunk_32k:
  dir: artifacts/sdedit/listen/aerofunk_32k
  tracks:
    input: input.flac
    master: original.flac
    apollo: apollo.flac
    a2sb: a2sb.flac
    sdedit n 0.50: sdedit_n_0.50.flac
    sdedit t 0.35: sdedit_t_0.35.flac
    sdedit hf10 t 0.35: sdedit_hf10t_0.35.flac
    sdedit hf20 t 0.35: sdedit_hf20t_0.35.flac

aerofunk_64k:
  dir: artifacts/sdedit/listen/aerofunk_64k
  tracks:
    input: input.flac
    master: original.flac
    apollo: apollo.flac
    a2sb: a2sb.flac
    sdedit n 0.50: sdedit_n_0.50.flac
    sdedit t 0.35: sdedit_t_0.35.flac
    sdedit hf10 t 0.35: sdedit_hf10t_0.35.flac
    sdedit hf20 t 0.35: sdedit_hf20t_0.35.flac

codec_32k:
  dir: artifacts/sdedit/listen/codec_32k
  tracks:
    input: input.flac
    master: original.flac
    apollo: apollo.flac
    a2sb: a2sb.flac
    sdedit n 0.50: sdedit_n_0.50.flac
    sdedit t 0.35: sdedit_t_0.35.flac
    sdedit hf10 t 0.35: sdedit_hf10t_0.35.flac
    sdedit hf20 t 0.35: sdedit_hf20t_0.35.flac

codec_64k:
  dir: artifacts/sdedit/listen/codec_64k
  tracks:
    input: input.flac
    master: original.flac
    apollo: apollo.flac
    a2sb: a2sb.flac
    sdedit n 0.50: sdedit_n_0.50.flac
    sdedit t 0.35: sdedit_t_0.35.flac
    sdedit hf10 t 0.35: sdedit_hf10t_0.35.flac
    sdedit hf20 t 0.35: sdedit_hf20t_0.35.flac
